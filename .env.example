# GitHub Security Advisories API
# Optional: Create a fine-grained personal access token with "Public repositories: Read-only" permission
# Or use gh CLI authentication (automatically detected)
# https://github.com/settings/tokens
GITHUB_TOKEN=
